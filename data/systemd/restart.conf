# Drop-in for the unit systemd-xdg-autostart-generator creates from
# /etc/xdg/autostart/io.github.wanleung.CosmicExtIme.desktop: if the input
# method crashes, bring it back instead of leaving the session without one.
[Service]
Restart=on-failure
RestartSec=2
