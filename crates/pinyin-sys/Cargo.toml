[package]
name = "pinyin-sys"
version.workspace = true
edition.workspace = true
license = "GPL-2.0-or-later"
rust-version.workspace = true
description = "Raw FFI bindings to libpinyin"
links = "pinyin"
build = "build.rs"

[dependencies]

[build-dependencies]
bindgen = "0.73"
pkg-config = "0.3"
